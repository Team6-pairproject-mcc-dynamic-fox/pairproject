<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div id="container">
        <%- include('./partial/navbar.ejs') %>
        <h1>CHOOSE HOSPITAL</h1>
        <table id="table">
            <thead>
                <th>NO</th>
                <th>NAME</th>
                <th>ADDRESS</th>
                <th>ACTION</th>
            </thead>
            <tbody>
                <% for (let i = 0; i < arr.length; i++) { %>
                    <tr>
                        <td><%= arr[i].id %></td>
                        <td><%= arr[i].name %></td>
                        <td><%= arr[i].address %></td>
                        <td><a href="/suspects/chooseHospital/<%= arr[i].id %>">[Y]</a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>

</body>
</html>